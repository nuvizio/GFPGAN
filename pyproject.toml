[project]
name = "gfpgan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10.16"
dependencies = [
    "basicsr>=1.4.2",
    "facexlib>=0.2.5",
    "lmdb",
    "numpy",
    "opencv-python",
    "pyyaml",
    "realesrgan",
    "scipy",
    "tb-nightly",
    "torch>=1.7",
    "torchvision",
    "tqdm",
    "yapf",
]
